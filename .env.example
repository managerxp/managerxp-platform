# client-app (CafeXP kiosk client) — copy the keys below into client-app/.env
CLIENT_PORT=9090
SERVER_APP_PORT=3334
BACKEND_BASE=http://localhost:5000

# server-app (CafeXP admin console) — copy the keys below into server-app/.env
BACKEND_PORT=3334
TOKEN_SERVER_PORT=9090
